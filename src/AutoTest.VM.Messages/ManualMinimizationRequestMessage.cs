using AutoTest.Messages;
using System.IO;

namespace AutoTest.VM.Messages
{
    public class ManualMinimizationRequestMessage : IMessage
    {
        public void SetDataFrom(BinaryReader reader)
        {
        }

        public void WriteDataTo(BinaryWriter writer)
        {
        }
    }
}
